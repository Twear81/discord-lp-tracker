const trackPlayer = async (): Promise<void> => {
	// Every 10 min, track all the players
	// Get all the servers

	// Get all the players for each server
};

const startTracking = async (): Promise<void> => {
	// Every 10 min, track all the players
	// Get all the servers

	// Get all the players for each server
};

export { startTracking, trackPlayer };